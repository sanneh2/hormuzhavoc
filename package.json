{
  "name": "hormuz-havoc",
  "version": "1.1.0",
  "description": "Arcade-style browser game: navigate the Strait of Hormuz and survive!",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "dependencies": {
    "phaser": "^3.60.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
